syntax = "proto3";

package its;

import "etsi_common.proto";

enum TrafficLightPhase {
  TRAFFICLIGHT_PHASE_UNKNOWN = 0;
  TRAFFICLIGHT_PHASE_GREEN = 1;
  TRAFFICLIGHT_PHASE_YELLOW = 2;
  TRAFFICLIGHT_PHASE_RED = 3;
}

message Spatem {
  ItsPduHeader header = 1;

  uint32 generation_delta_time = 2;

  ReferencePosition reference_position = 3;
  TrafficLightPhase trafficlight_phase = 4;
  uint32 remaining_seconds = 5;
}